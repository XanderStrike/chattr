<div class="header">
	<h1><%= @user.name %></h1>
	<h2>
		<%= (@user == current_user) ? "Who's awesome? You're awesome." : "Certainly nobody important." %>
	</h2>
</div>

<div class="content">
	<p>Username: <%= @user.name %></p>
	<p>Email: <%= @user.email if @user.email %></p>
</div>
